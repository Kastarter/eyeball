---
title: "Get an execution"
description: "GET /v1/executions/:id for public status, output, or normalized error."
---

```bash
curl "$EYEBALL_EXECUTOR_URL/v1/executions/exe_01JZ6WA0Q73ZQ5B51SRYB6M4Z8" \
  -H "Authorization: Bearer $EYEBALL_API_KEY"
```

The route returns the project-scoped public execution record. Canonical input, connection selection, idempotency keys, and internal project fields are deliberately not exposed:

```json
{
  "executionId": "exe_01JZ6WA0Q73ZQ5B51SRYB6M4Z8",
  "tool": "gmail.search_emails",
  "toolVersion": "1.0.0",
  "catalogVersion": "1.1",
  "status": "succeeded",
  "userId": "demo_user",
  "createdAt": "2026-07-17T12:00:00.000Z",
  "output": {
    "emails": [],
    "nextPageToken": "page_2"
  },
  "latencyMs": 31
}
```

A missing or cross-project ID returns `not_found`. Poll until `succeeded` or `failed`; terminal records do not change.
