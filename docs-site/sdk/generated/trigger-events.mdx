---
title: "Trigger events API"
description: "Read redacted project trigger history and current delivery outcomes without provider payloads or credentials."
---

<!-- DO NOT EDIT: generated by pnpm docs:sdk; SDK checksum 320944d14c04bf61. -->

Read redacted project trigger history and current delivery outcomes without provider payloads or credentials.

## `TriggerEventsClient`

Project-scoped, metadata-only trigger arrival and delivery history.

### `list`

Lists redacted project-scoped trigger arrivals newest first with aggregate and per-target delivery outcomes.

This resource never inherits the client's default `userId`; user-pinned keys are rejected by the executor.

```ts no-check
list(options?: ListTriggerEventsOptions): Promise<TriggerEventPage>
```

| Parameter | Type | Required | Description |
|---|---|---|---|
| `options` | <code>ListTriggerEventsOptions</code> | No | Opaque cursor, page size, and exact subscription or canonical-trigger filters. |

**Returns:** <code>Promise&lt;TriggerEventPage&gt;</code>

**Throws**

- EyeballError when options are invalid, the key is user-pinned, or the executor request fails.

**Example**

```ts
import { Eyeball } from "@eyeball/sdk";

const eyeball = new Eyeball({
  apiKey: process.env.EYEBALL_API_KEY!,
  baseUrl: process.env.EYEBALL_EXECUTOR_URL!,
});

let page = await eyeball.triggerEvents.list({
  trigger: "slack.message_received",
  limit: 50,
});
for (;;) {
  for (const event of page.triggerEvents) {
    console.log(event.deliveryStatus);
    for (const target of event.deliveryTargets) {
      console.log(target.endpointId, target.status);
    }
  }
  if (page.nextCursor === undefined) break;
  page = await eyeball.triggerEvents.list({
    trigger: "slack.message_received",
    limit: 50,
    cursor: page.nextCursor,
  });
}
```

## `createTriggerEventArrivalId`

Creates a metadata-history `trgevt_*` arrival ID; a seed makes tests deterministic.

```ts no-check
createTriggerEventArrivalId(seed?: string | undefined): `trgevt_${string}`
```

| Parameter | Type | Required | Description |
|---|---|---|---|
| `seed` | <code>string &#124; undefined</code> | No | — |

**Returns:** <code>`trgevt_$&#123;string&#125;`</code>

## `isTriggerEventArrivalId`



```ts no-check
isTriggerEventArrivalId(value: string): boolean
```

| Parameter | Type | Required | Description |
|---|---|---|---|
| `value` | <code>string</code> | Yes | — |

**Returns:** <code>boolean</code>

## Types

### `ListTriggerEventsOptions`

Project-scoped filters and cursor controls for redacted trigger-event history.
This resource never inherits the client's default `userId`.

```ts no-check
export interface ListTriggerEventsOptions {
  cursor?: string;
  limit?: number;
  subscriptionId?: TriggerSubscriptionId;
  trigger?: QualifiedTriggerName;
}
```

| Property | Type | Required | Readonly | Description |
|---|---|---|---|---|
| `cursor` | <code>string</code> | No | No | — |
| `limit` | <code>number</code> | No | No | — |
| `subscriptionId` | <code>`trgsub_$&#123;string&#125;`</code> | No | No | — |
| `trigger` | <code>`$&#123;string&#125;.$&#123;string&#125;`</code> | No | No | — |

### `TriggerEventArrivalId`



```ts no-check
export type TriggerEventArrivalId = `trgevt_${string}`;
```

### `TriggerEventDedupStatus`



```ts no-check
export type TriggerEventDedupStatus = "accepted" | "duplicate";
```

### `TriggerEventDeliveryMode`



```ts no-check
export type TriggerEventDeliveryMode = "push" | "polling";
```

### `TriggerEventDeliveryStatus`



```ts no-check
export type TriggerEventDeliveryStatus =
  | "not_enqueued"
  | "admission_failed"
  | "selecting"
  | "no_targets"
  | "pending"
  | "delivering"
  | "succeeded"
  | "failed"
  | "partial";
```

### `TriggerEventDeliveryTarget`



```ts no-check
export interface TriggerEventDeliveryTarget {
  endpointId: string;
  deliveryId: string;
  status: WebhookDeliveryStatus;
}
```

| Property | Type | Required | Readonly | Description |
|---|---|---|---|---|
| `deliveryId` | <code>string</code> | Yes | No | — |
| `endpointId` | <code>string</code> | Yes | No | — |
| `status` | <code>WebhookDeliveryStatus</code> | Yes | No | — |

### `TriggerEventRecord`

Metadata-only, project-authorized history for one normalized provider arrival.

```ts no-check
export interface TriggerEventRecord {
  arrivalId: TriggerEventArrivalId;
  eventId: string;
  subscriptionId: TriggerSubscriptionId;
  trigger: QualifiedTriggerName;
  deliveryMode: TriggerEventDeliveryMode;
  receivedAt: string;
  occurredAt: string;
  dedupStatus: TriggerEventDedupStatus;
  deliveryStatus: TriggerEventDeliveryStatus;
  requestedWebhookEndpointIds: readonly string[];
  deliveryTargets: readonly TriggerEventDeliveryTarget[];
  expiresAt: string;
}
```

| Property | Type | Required | Readonly | Description |
|---|---|---|---|---|
| `arrivalId` | <code>`trgevt_$&#123;string&#125;`</code> | Yes | No | — |
| `dedupStatus` | <code>TriggerEventDedupStatus</code> | Yes | No | — |
| `deliveryMode` | <code>TriggerEventDeliveryMode</code> | Yes | No | — |
| `deliveryStatus` | <code>TriggerEventDeliveryStatus</code> | Yes | No | — |
| `deliveryTargets` | <code>readonly TriggerEventDeliveryTarget[]</code> | Yes | No | — |
| `eventId` | <code>string</code> | Yes | No | — |
| `expiresAt` | <code>string</code> | Yes | No | — |
| `occurredAt` | <code>string</code> | Yes | No | — |
| `receivedAt` | <code>string</code> | Yes | No | — |
| `requestedWebhookEndpointIds` | <code>readonly string[]</code> | Yes | No | — |
| `subscriptionId` | <code>`trgsub_$&#123;string&#125;`</code> | Yes | No | — |
| `trigger` | <code>`$&#123;string&#125;.$&#123;string&#125;`</code> | Yes | No | — |

### `TriggerEventPage`



```ts no-check
export interface TriggerEventPage {
  triggerEvents: readonly TriggerEventRecord[];
  nextCursor?: string;
}
```

| Property | Type | Required | Readonly | Description |
|---|---|---|---|---|
| `nextCursor` | <code>string</code> | No | No | — |
| `triggerEvents` | <code>readonly TriggerEventRecord[]</code> | Yes | No | — |
