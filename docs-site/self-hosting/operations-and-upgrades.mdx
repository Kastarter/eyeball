---
title: "Operations and upgrades"
description: "Replace in-memory defaults, protect public edges, and deploy compatible packages together."
---

Before production:

- implement durable `ExecutionStore` and queue boundaries;
- persist idempotency correlation and immutable terminal records;
- run the executor, gateway, and dashboard behind TLS and authentication;
- rate-limit bearer keys and MCP sessions;
- configure the executor's redacted logs and optional OpenTelemetry export, then add deployment metrics for queue age and provider saturation;
- keep provider credentials out of custom logs, traces, errors, collector configuration, and client bundles;
- back up execution/voice history according to retention policy;
- replace the process-local webhook stores/queue, retain pending jobs across restarts, and alert on exhausted deliveries; signing and retry semantics are fixed by RFC 001.

The dashboard uses a server-side proxy; keep `EYEBALL_API_KEY` server-only. Mockhouse and `/_mock` must stay on loopback or an isolated test network.

## Upgrade order

1. Update compatible core, catalog, toolkits, executor, gateway, SDK, and dashboard revisions.
2. Regenerate toolkit docs and review schema/version diffs.
3. Replay mock contract tests, including mutation idempotency and worker recovery.
4. Deploy executor support for additive contracts.
5. Deploy gateway/dashboard and then SDK clients.
6. Monitor normalized error shifts and queue state.

Tool and catalog versions in each execution provide the audit trail; do not discard them during migration.
